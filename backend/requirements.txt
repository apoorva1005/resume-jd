# Loose lower bounds so a fresh install resolves current versions.

fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.18

pymongo>=4.13          # includes AsyncMongoClient + async GridFS
chromadb>=1.0          # vector index for resume/JD embeddings
pydantic[email]>=2.9   # [email] pulls in email-validator, needed by EmailStr
pydantic-settings>=2.6

bcrypt>=4.2
python-jose[cryptography]>=3.3

pdfplumber>=0.11
docx2txt>=0.8

sentence-transformers>=3.3
scikit-learn>=1.5
joblib>=1.4
numpy>=1.26

httpx>=0.27

pytest>=8.3
mongomock_motor>=0.0.34
